# Built-in Public Strategies

These files define the public strategy surface shipped with the repository.

- `c5.json`: baseline public strategy
- `sat06.json`: public satellite variant

Both strategies are intended to run with stock-only filtering. They should not
open positions in `NEPSE` or `SECTOR::...` proxy symbols.
